
type TransactionTableProps = {
  burnTransactions: string;
  coinData: {};
}

const TransactionTable = ({ burnTransactions, coinData } : TransactionTableProps) => (
  <TransactionTableStyled>
    {/* Transaction table */}
  </TransactionTableStyled>
);

export default TransactionTable;