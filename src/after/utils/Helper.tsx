import styled from "styled-components";

export const BurnPageStyled = styled.div``;

// Styled component for DashboardLayout
export const DashboardLayoutStyled = styled.div``;

// Enum for burn transaction progress
export enum BurnTxProgress {
  default = "Burn App Tokens",
  burning = "Burning...",
}